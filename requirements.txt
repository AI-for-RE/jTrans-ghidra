# Verified on Python 3.14 with pyghidra 3.1.0, torch 2.13.0, transformers 5.15.0.

# Feature extraction (datautils/).
# pyghidra also needs Ghidra 11.3+ and a JDK 21+ installed separately, with
# GHIDRA_INSTALL_DIR pointing at the Ghidra directory; pip cannot provide those.
pyghidra
pyelftools
networkx
tqdm

# Training and evaluation (finetune.py, eval_save.py, fasteval.py, data.py).
torch
transformers
numpy
wandb

# Named by the README but not imported anywhere in this repo. It pins older
# transformers releases, so installing it may downgrade the version used above.
simpletransformers
